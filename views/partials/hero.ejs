<section id="hero-video">
  <div class="hv-video-wrap">
    <video autoplay muted loop playsinline>
      <source src="/img/M.mp4" type="video/mp4">
    </video>
  </div>
  <div class="hv-overlay"></div>
  <div class="hv-scroll-hint">
    <div class="hv-scroll-line"></div>
    Scroll
  </div>
</section>

<!-- ── Hero text — separate section with scroll reveal ── -->
<section id="hero-text" class="sec-zoom">
  <div class="wrap">
    <div class="ht-inner">

      <h1 class="hv-title hd-zoom" style="color:var(--ink)">Todos los modelos<br/><em>en un solo lugar</em></h1>
      <p class="hv-sub rv" style="color:var(--ink3)">Accede a GPT-4, Claude, Gemini y más con una sola suscripción. Sin cambiar de pestaña, sin múltiples pagos.</p>
      <div class="hv-actions rv">
        <button class="btn btn-amber lg" onclick="openModal('pro')">Try MultIA →</button>
      </div>
    </div>
  </div>
</section>
